spring:
  application:
    name: llm-pdd-refund

  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY:your-dashscope-api-key}
      chat:
        options:
          model: qwen-plus
          temperature: 0.7

server:
  port: 8084
