spring:
  application:
    name: llm-spring-ai-alibaba

  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY:your-api-key}
      chat:
        options:
          model: qwen-turbo
          temperature: 0.7

server:
  port: 8082
