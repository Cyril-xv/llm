spring:
  application:
    name: llm-langchain4j

langchain4j:
  open-ai:
    api-key: ${OPENAI_API_KEY:your-api-key}
    base-url: ${OPENAI_BASE_URL:https://api.openai.com}
    chat-model:
      model-name: gpt-4o-mini
      temperature: 0.7

server:
  port: 8083
